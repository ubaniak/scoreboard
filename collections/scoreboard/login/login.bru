meta {
  name: login
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/login
  body: json
  auth: bearer
}

auth:bearer {
  token: aasdsad
}

body:json {
  {
    "role": "admin",
    "code": "44166"
  }
}
