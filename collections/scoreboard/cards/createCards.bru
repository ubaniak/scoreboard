meta {
  name: createCards
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/cards
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "foo"
  }
}
